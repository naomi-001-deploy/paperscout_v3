streamlit>=1.37,<2.0
pandas>=2.0,<3.0
httpx>=0.27,<0.28

# Excel: nur EINE Engine aktiv lassen
xlsxwriter>=3.2,<4.0
# openpyxl>=3.1,<4.0   # (Alternative, falls du lieber openpyxl nutzt)

# HTML/Parsing (falls genutzt; ungefährlich):
beautifulsoup4>=4.12,<5.0
lxml>=5.2,<6.0

# KI-Fallback (nur wenn du ihn wirklich nutzt):
# openai>=1.40,<2.0
